skip_list:
  - role-name
  - package-latest  # Package installs should not use latest
  - schema[meta]    # Skip meta schema validation
exclude_paths:
  - molecule/
  - .github/
  - tasks/RedHat7.yml
